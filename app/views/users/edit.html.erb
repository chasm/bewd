<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @user do |f| %>
      <fieldset>
        <legend>Edit <%= @user.name %></legend>
        <%= render partial: 'form', locals: { f: f } %>
      </fieldset>
    <% end %>
  </div>
</div>
